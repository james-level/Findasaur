import { StyleSheet, Text, View, ImageBackground } from 'react-native';

const ExploreButtonContainerStyle = StyleSheet.create({
  content: {
    width: '100%',
    backgroundColor: 'black',
    height: 100,
    marginBottom: 20,
    backgroundColor: 'black',
    justifyContent: 'center',
    alignItems: 'center',
  },
});

export default ExploreButtonContainerStyle;
